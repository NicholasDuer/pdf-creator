plugins {
    id 'application'
}

apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {

    // unit test framework
    testImplementation group: 'org.jmock', name: 'jmock', version: '2.5.1'
    testImplementation "junit:junit:4.12"
}
